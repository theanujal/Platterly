import type { MetadataRoute } from "next";

/**
 * Chunk 1 Group 1.4: empty scaffold. Chunk 8 populates this with one entry
 * per published tenant storefront once that table exists — this file's job
 * for Chunk 1 is just to prove the route resolves correctly.
 */
export default function sitemap(): MetadataRoute.Sitemap {
  return [];
}
