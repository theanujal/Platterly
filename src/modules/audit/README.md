# audit

Owning chunk: Chunk 2 — Foundation B (interface, `src/lib/audit/`, consumed by every later chunk from Chunk 9 onward).

This is a §57 boundary-map stub, not where the code lives — see `src/lib/audit/audit.ts`. `src/modules/` is reserved for actual domain/business modules (customers, events, orders, menus, etc.) starting Chunk 6/9.
